{
  "name": "divi-child",
  "version": "1.0.0",
  "description": "An attempt to install the MAterial Design Components in to the divi theme",
  "main": "npm help json",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Luke Marshall",
  "license": "ISC",
  "devDependencies": {
    "gulp": "^3.9.1",
    "material-design-lite": "^1.3.0"
  },
  "dependencies": {
    "sass": "^0.5.0"
  }
}
